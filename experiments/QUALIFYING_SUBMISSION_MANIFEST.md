# Qualifying submission manifest

## Frozen candidate

- Branch: `qualifying-brake-search`
- Controller/config freeze: `fca8332`
- Clean source base: `ee3040254d41c8e2d6fb501c8901ea94c34694d6`
- Reproducible export config: `experiments/configs/qualifying_brake_known_good.json`
- Resolved experiment ID:
  `qualifying_brake.two_event_combination=1__qualifying_brake.wp2501_left_foot_percent=10__qualifying_brake.wp794_lap3_final_tick=1__runtime.low_bandwidth_sensor_mode=1--a9af10edf771`

The behavior consists only of WP1232 release -1, WP2501 release -2, lap-3
WP794 final-tick suppression, and WP2501 throttle 0.10 on the final five
applied brake ticks. Rejected Spring mu treatments and the unstable conditional
WP1232 re-brake treatment are not enabled.

## Reproduce and validate

Inspect the exact generated patch without CARLA:

```bash
python3 -m experiments.harness.cli dry-run \
  experiments/configs/qualifying_brake_known_good.json
```

Run/resume the five forced-clean final campaign:

```bash
python3 -m experiments.harness.cli causal \
  experiments/configs/qualifying_brake_final_validation.json
```

The 2026-08-19 attempt was infrastructure-blocked before any vehicle run:
CARLA's port was listening after SSM restart, but `get_sensor_token` failed for
all 48 readiness probes. This is not a controller reliability observation.

## Files required in the submitted controller bundle

Submit the resolved versions generated by the known-good config, preserving
their relative paths:

- `competition_code/submission.py`
- `competition_code/LateralController.py`
- `competition_code/ThrottleController.py`
- `competition_code/SpeedData.py`
- `competition_code/qualifying_brake_combination.py`
- `competition_code/qualifying_brake_wp2501_left_foot.py`
- `competition_code/qualifying_brake_wp794_lap3.py`
- `competition_code/waypoints/waypointsPrimary.npz`
- a writable `competition_code/debugData/` directory (or its existing
  `debugData.json`) because the inherited submission writes its exit log there

Do not submit an unmodified checkout of `fca8332`'s `competition_code/` alone:
the validated controller is a reproducible parameterized export from
`ee30402`, and the three generated override modules must be included.

The experiment-only telemetry module, harness, raw telemetry, rejected Spring
configs, and analysis reports are provenance and are not required by the race
submission runtime.

A resolved, compile-checked bundle was generated locally at
`experiment_results/submission/fca8332-qualifying-submission.zip` (ignored by
Git). SHA-256:
`f9a1123168d29b599a3a2d1b084d7a758a769291fb8eaf6bd909fcdbdb1a2b54`.
Its per-file checksums are in
`experiment_results/submission/fca8332/SHA256SUMS`.
